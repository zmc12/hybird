<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.hybird.mapper.AbsentMapper">
    <resultMap id="BaseResultMap" type="com.jsut.hybird.pojo.Absent">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="reason" jdbcType="VARCHAR" property="reason"/>
        <result column="start_time" jdbcType="VARCHAR" property="startTime"/>
        <result column="end_time" jdbcType="VARCHAR" property="endTime"/>
        <result column="result" jdbcType="VARCHAR" property="result"/>
    </resultMap>


    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id,name, reason, start_time, end_time,result,college
    </sql>


    <select id="selectAll"  resultMap="BaseResultMap">
        select  * from absent where result="未审批" order by id asc;
    </select>


    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select * from absent where id=#{id};
    </select>

    <update id="updateById" parameterType="com.jsut.hybird.pojo.Absent" >
        update  absent set result=#{result} where id=#{id};
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from absent where id=#{id};
    </delete>

    <insert id="insertT" parameterType="com.jsut.hybird.pojo.Absent">
        insert into absent(name,reason,start_time,end_time,result) values (#{name},#{reason},#{startTime},#{endTime},#{result});
    </insert>

    <select id="selectByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        select * from absent where name=#{name};
    </select>



</mapper>