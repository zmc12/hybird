<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.hybird.mapper.StudentMapper">
    <resultMap id="BaseResultMap" type="com.jsut.hybird.pojo.Student">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="college" jdbcType="VARCHAR" property="college"/>
        <result column="grade" jdbcType="VARCHAR" property="grade"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id,name, user_name, password, college,grade
    </sql>


    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from student where id=#{id}
    </select>



    <update id="updateById" parameterType="com.jsut.hybird.pojo.Student">
        update student set name=#{name},user_name=#{userName},password=#{password},college=#{college},grade=#{grade} where id=#{id};
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from student where id=#{id};
    </delete>


    <insert id="insertT" parameterType="com.jsut.hybird.pojo.Teacher">
        insert into student (name,user_name,password,college,grade) values (#{name},#{userName},#{password},#{college},#{grade});
    </insert>

    <select id="selectAll"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from student order by id desc;
    </select>


    <select id="selectByUser"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from student where user_name=#{userName} and password=#{password}
    </select>


    <select id="selectByNumber" resultMap="BaseResultMap" parameterType="java.lang.String">
        select  <include refid="Base_Column_List" /> from student where user_name=#{number};
    </select>


    <select id="selectByGrade" parameterType="java.lang.String"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from student where grade=#{grade};
    </select>
</mapper>