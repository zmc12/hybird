<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.hybird.mapper.ScoreMapper">
    <resultMap id="BaseResultMap" type="com.jsut.hybird.pojo.Score">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="score" jdbcType="INTEGER" property="score"/>
        <result column="subject" jdbcType="VARCHAR" property="subject"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id,name, score, subject
    </sql>

    <select id="selectAll"  resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from score order by id desc;
    </select>

    <select id="selectById" parameterType="java.lang.Integer"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from score where id=#{id};
    </select>

    <update id="updateById" parameterType="com.jsut.hybird.pojo.Score">
        update  score set score=#{score} where name=#{name} and subject=#{subject};
    </update>

    <delete id="deleteById"  parameterType="java.lang.Integer" >
        delete from score where id=#{id};
    </delete>

    <insert id="insertT" parameterType="com.jsut.hybird.pojo.Score">
        insert into score (name,score,subject) values (#{name},#{score},#{subject});
    </insert>



    <select id="ifHas" parameterType="com.jsut.hybird.pojo.Score" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from score where name=#{name} and subject=#{subject};
    </select>

    <select id="selectSubject" resultMap="BaseResultMap">
        select distinct subject from score;
    </select>


    <select id="selectByGrade" resultMap="BaseResultMap">
        select  a.* from score a,(select name from student where grade=#{grade}) b where a.name=b.name and  subject=#{subject} order by id asc;
    </select>


    <select id="selectByName" parameterType="java.lang.String"  resultMap="BaseResultMap">
        select * from score where name=#{name};
    </select>
</mapper>