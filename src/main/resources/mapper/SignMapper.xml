<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.hybird.mapper.SignMapper">
    <resultMap id="BaseResultMap" type="com.jsut.hybird.pojo.Sign">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="method" jdbcType="VARCHAR" property="method"/>
        <result column="stuation" jdbcType="VARCHAR" property="stuation"/>
        <result column="temperature" jdbcType="VARCHAR" property="temperature"/>
        <result column="fire" jdbcType="VARCHAR" property="fire"/>
        <result column="smoke" jdbcType="VARCHAR" property="smoke"/>
        <result column="wet" jdbcType="VARCHAR" property="wet"/>
        <result column="time" jdbcType="VARCHAR" property="time"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->8
        id, name, method ,stuation,temperature,fire,smoke,wet,time
    </sql>

    <select id="selectSign"  resultMap="BaseResultMap">
        select <include refid="Base_Column_List" />  from sign where stuation="已签到";
    </select>

    <select id="selectNoSign"  resultMap="BaseResultMap">
        select <include refid="Base_Column_List" />  from sign where stuation="未签到";
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from sign order by id desc ;
    </select>

    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select <include refid="Base_Column_List" /> from sign where id =#{id};
    </select>

    <insert id="insertT" parameterType="com.jsut.hybird.pojo.Sign">
        insert into sign(name,method,stuation,temperature,fire,smoke,wet,time) values (#{name},#{method},#{stuation},#{temperature},#{fire},#{smoke},#{wet},#{time});
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from sign where id=#{id};
    </delete>

    <update id="updateById" parameterType="com.jsut.hybird.pojo.Sign">
           update sign set stuation=#{已签到} where id={id};
    </update>

    <select id="selectByGrade" resultMap="BaseResultMap" parameterType="java.lang.String">
        select a.*  from sign a,(select name from student  where grade=#{grade}) b where a.name=b.name and  a.time=#{time};
    </select>

</mapper>