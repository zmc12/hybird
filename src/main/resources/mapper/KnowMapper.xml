<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.hybird.mapper.KnowMapper">
    <resultMap id="BaseResultMap" type="com.jsut.hybird.pojo.Know">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="nid" jdbcType="INTEGER" property="nid"/>
        <result column="situation" jdbcType="VARCHAR" property="situation"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, nid, situation,name
    </sql>


    <insert id="insert" parameterType="com.jsut.hybird.pojo.Know">
        insert into know(nid,name,situation) values (#{nid},#{name},#{situation});
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from know where nid=#{nid};
    </delete>


    <select id="selectStudent" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from know  where situation="未确认" and nid=#{id} order by id desc;
    </select>

    <update id="updateY" >
        update know set situation="已确认" where nid=#{id} and name=#{name};
    </update>

</mapper>