<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.hybird.mapper.MarketMapper">
    <resultMap id="BaseResultMap" type="com.jsut.hybird.pojo.Market">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="text" jdbcType="VARCHAR" property="text"/>
        <result column="contact" jdbcType="VARCHAR" property="contact"/>
        <result column="path" jdbcType="VARCHAR" property="path"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="price" jdbcType="INTEGER" property="price"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, text, contact,path,title,price,user_name
    </sql>
    <select id="selectAll"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from market order by id desc;
    </select>
    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select  <include refid="Base_Column_List" /> from market where id=#{id};
    </select>


    <insert id="insertT" parameterType="com.jsut.hybird.pojo.Market">
        insert into market(text,contact,path,price,title,user_name) values (#{text},#{contact},#{path},#{price},#{title},#{userName});
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from market where id=#{id};
    </delete>
    <update id="updateById" parameterType="com.jsut.hybird.pojo.Market">
        update  market set text=#{text},contact=#{contact},path=#{path},title=#{title},price=#{price},user_name=#{userName} where id=#{id};
    </update>

    <select id="selectByUN" resultMap="BaseResultMap" parameterType="java.lang.String">
        select  <include refid="Base_Column_List" /> from market where user_name=#{userName};
    </select>
</mapper>