<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.hybird.mapper.DomMapper">
    <resultMap id="BaseResultMap" type="com.jsut.hybird.pojo.Dom">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="number" jdbcType="VARCHAR" property="number"/>
        <result column="score" jdbcType="INTEGER" property="score"/>
        <result column="students" jdbcType="VARCHAR" property="students"/>
        <result column="fix" jdbcType="VARCHAR" property="fix"/>
        <result column="process" jdbcType="VARCHAR" property="process"/>
        <result column="grade" jdbcType="VARCHAR" property="grade"/>
        <result column="term" jdbcType="VARCHAR" property="term"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, number, score ,students,fix,process,grade,term
    </sql>

    <select id="selectAll"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from dom order by id desc;
    </select>


    <insert id="insertT" parameterType="com.jsut.hybird.pojo.Dom">
        insert into dom(number,score,students) values (#{number},#{score},#{students});
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from dom where id=#{id};
    </delete>

    <update id="updateById" parameterType="com.jsut.hybird.pojo.Dom">
        update  dom set number=#{number},score=#{score},students=#{students} where id=#{id};
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from dom where id=#{id};
    </select>


    <select id="selectByGrade" resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from dom where grade=#{grade} and term=#{term} order by id desc;
    </select>

</mapper>